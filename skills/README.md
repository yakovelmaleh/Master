# Repository-local skills

Skills in this folder apply only to the `Master` repository.

Each skill has its own folder and a `SKILL.md` file describing when it should
run, required safety checks, workflow, outputs, and cleanup behavior.

Available skills:

- `investigate-failed-dataset` - diagnose a failed cluster dataset run,
  recommend actions, archive the issue, and safely clean investigation files.
